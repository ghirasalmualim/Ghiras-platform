# STEP 1D — مراجعة `2daca4f` المنشور

> قراءة فقط · لم يُعدَّل ولم يُرجَع ولم يُنشر شيء

---

## ① الديف — ما غيّره فعلًا

**ملفان لا غير.** `src/app/login/page.tsx` (+24 −8) · `src/lib/supabase/client.ts` (+33 −0)

### `client.ts` — BEFORE

```ts
export function createClient() {
  return createBrowserClient(url, anonKey, {
    cookieOptions: {
      name: 'sb-ghiras-auth',
      path: '/',
      ...(process.env.NODE_ENV === 'production'
        ? { domain: '.ghiras-edu.com' } : {}),
    },
  });
}
```

### `client.ts` — AFTER

```ts
let shadowCleared = false;
function clearShadowingCookies() {
  if (typeof document === 'undefined' || shadowCleared) return;
  shadowCleared = true;
  const names = document.cookie
    .split(';')
    .map((c) => c.split('=')[0].trim())
    .filter((n) => n === 'sb-ghiras-auth' || n.startsWith('sb-ghiras-auth.'));
  for (const n of names) document.cookie = `${n}=; Max-Age=0; path=/`;
}

export function createClient() {
  clearShadowingCookies();          // ← السطر الوحيد المضاف داخل الدالّة
  return createBrowserClient(/* … بلا تغيير … */);
}
```

⚠️ **وتصحيحٌ لخطأ في وثيقتي السابقة:** قلت إن `2daca4f` «أضاف `path:'/'`».
**غير صحيح.** `cookieOptions` لم يُمسّ في هذا الالتزام إطلاقًا —
`path` و`domain` أُضيفا في `60b6d67` يوم ١٦ أغسطس.

### `login/page.tsx` — BEFORE

```ts
void supabase.from('login_logs').insert({ … });
void supabase.rpc('touch_last_active');
router.push(next);
```

### `login/page.tsx` — AFTER

```ts
try {
  await Promise.all([
    supabase.from('login_logs').insert({ … }),
    supabase.rpc('touch_last_active'),
  ]);
} catch { /* … */ }
```

⚠️ **ولا شيء في هذا الملف يمسّ الكوكي أو الجلسة.**

---

## ② ماذا يُمسح بالضبط

| السؤال | الجواب من الكود |
|---|---|
| الأسماء المستهدَفة | `sb-ghiras-auth` **و** كل ما يبدأ بـ`sb-ghiras-auth.` |
| `path` في أمر الحذف | `/` صراحةً |
| `domain` في أمر الحذف | **لا شيء** ⇒ يصيب **المحصورة بالمضيف وحدها** |
| متى يُنفَّذ | أول نداء لـ`createClient()` بعد تحميل الصفحة |
| مرّة واحدة أم كل تحميل | **مرّة واحدة لكل تحميلٍ كامل** (`shadowCleared` يعيش مع الوحدة) |
| قبل `createBrowserClient` أم بعده | **قبله** — السطر الأول في الدالّة |
| قبل استعادة الجلسة أم بعدها | **قبلها** — المكتبة لم تُنشأ بعد |

**والأجزاء المرقّمة:** المكتبة تُقسّم عند `MAX_CHUNK_SIZE = 3180`
بالنمط `^(.*)[.](0|[1-9][0-9]*)$` — أي `sb-ghiras-auth.0` و`.1`.
**والمرشِّح يغطّيها** ✅ (وهو أوسع منها قليلًا، بلا أثرٍ عملي).

---

## ③ هل يمسح جلسةً صحيحة؟

# ⚠️ POSSIBLE — نعم، في التطوير المحلي

### الإنتاج: لا — ولا هدف له أصلًا

**تسلسل الالتزامات يُثبت أن الهدف لا وجود له:**

| الوقت | الالتزام | ماذا فعل |
|---|---|---|
| ٠٨-١٦ **١٢:١١** | `88756ed` | النطاق `.ghiras-edu.com` ← الخادم + الوسيط |
| ٠٨-١٦ **١٤:١٤** | `60b6d67` | النطاق + `path:'/'` ← المتصفح |
| ٠٨-١٦ **٢٣:٣٧** | `63dcb25` | **وُلد الاسم `sb-ghiras-auth`** ← الثلاثة معًا |

⚠️ **فالاسم `sb-ghiras-auth` وُلد بعد أن صار كل كاتبٍ يُلحق النطاق.**
**ولم يوجد قطُّ في الإنتاج بصيغةٍ محصورةٍ بالمضيف** — ولا سبيل إلى
إنشائه كذلك:

- **الإنتاج** (`www.ghiras-edu.com`): `NODE_ENV=production` ⇒ نطاقٌ دائمًا
- **معاينات Vercel** (`*.vercel.app`): النطاق يُلحَق فيرفضه المتصفح لعدم المطابقة ⇒ **لا كوكي البتّة**
- **الاستوديو** (`studio.ghiras-edu.com`): يكتب بالنطاق كذلك — وتعليقُه يشهد أن التظليل كان مع **الاسم القديم**

**وكوكي الاسم القديم `sb-<ref>-auth-token`** قد تبقى محصورةً على
أجهزة قديمة — **لكنها اسمٌ آخر فلا تُظلّل، والمرشِّح لا يمسّها.**
**وهذا بعينه ما عالجه `63dcb25` بالتسمية.**

### التطوير المحلي: **نعم — يمسح الجلسة الصحيحة**

على `localhost` تكون `NODE_ENV=development` ⇒ **لا نطاق** ⇒ الكوكي
الصحيحة **محصورةٌ بالمضيف** — وهي بعينها ما يمسحه الكود.

```
تحميل كامل → clearShadowingCookies() تمسح الكوكي الصحيحة
            → createBrowserClient يقرأ فيجد فراغًا
            → لا جلسة
```

⚠️ **فكل تحديثٍ للصفحة محليًا يُخرج المطوِّرة.**

⚠️ **وأثره أخطر ممّا يبدو:** المعاينات لا تصادق أصلًا (W11)، فكان
المحلّي **آخر بيئةٍ يُختبر فيها الدخول** — وهذا الالتزام أغلقها.

---

## ④ تسلسل ما يقع لمستخدمة قائمة (إنتاج)

1. تحميلٌ كامل ⇒ الوسيط يقرأ كوكي النطاق ويجدّدها عند الحاجة ✅
2. الترطيب ⇒ أول `createClient()` يستدعي `clearShadowingCookies()`
3. تقرأ `document.cookie` فترى `sb-ghiras-auth` (المكتبة تضع `httpOnly:false`)
4. تكتب `sb-ghiras-auth=; Max-Age=0; path=/` **بلا نطاق**
   ⇒ يُنشئ المتصفح مُدخلًا **محصورًا بالمضيف** منتهيًا فيُسقطه فورًا.
   **وكوكي `.ghiras-edu.com` مُدخلٌ آخر في الجرّة — لا يُمسّ**
   (المفتاح في RFC 6265 هو الثلاثي: الاسم + النطاق + المسار)
5. `createBrowserClient` يقرأ ⇒ كوكي النطاق قائمة ⇒ **الجلسة تُستعاد** ✅

| السؤال | الجواب |
|---|---|
| تبقى الجلسة؟ | ✅ نعم |
| تُعاد كتابتها؟ | فقط إن جدّدها الوسيط — لا علاقة للرقعة |
| يحتاج تحديثًا؟ | ❌ لا |
| قد يظهر خروج؟ | ❌ لا في الإنتاج · ✅ **نعم محليًا** |
| تعارضُ توقيت؟ | ❌ لا — تجري مرّة، **قبل** إنشاء المكتبة، وقبل الدخول لا بعده |

---

## ⑤ الحكم

# C — PATCH MAY AFFECT VALID SESSIONS

**وتفصيله:**

- **في الإنتاج: `B`** — سليمٌ بلا ضرر، **وبلا مبرّر**: يمسح كوكيًّا
  ثبت أنها لم توجد قط.
- **في التطوير: `C`** — يمسح الجلسة الصحيحة في كل تحميل.

⚠️ **والأهم: الفرضية التي بُني عليها لم تعد `NOT CONFIRMED` بل
`DISPROVED`.** تسلسل الالتزامات يمنع نشوء الكوكي المُظلِّلة بهذا
الاسم. **فما أصلحتُه ليلتها لم يكن العطب.**

**ولا أعدّل شيئًا — القرار قرارك.**

---

## ⑥ الالتزامات الستة

| الالتزام | ما فعل | التصنيف | السبب |
|---|---|---|---|
| `2b988ab` | شاشة تأكيد التسجيل | ✅ **Safe improvement** | واجهةٌ فقط، لا تمسّ مصادقة |
| `0d63469` | رسائل خطأٍ مفصَّلة | ✅ **Safe improvement** | يعرض رمز الخطأ للمستخدمة — إفشاءٌ ضئيل ومقبول |
| `b43e2a9` | التقاط خطأ قراءة الملف | ✅ **Safe improvement** | يمنع اتهام حسابٍ سليم |
| `2daca4f` | مسح الكوكي + `await` للسجلّ | 🔴 **Potential production risk** | الفرضية مُبطَلة · يكسر الجلسة محليًا · **و`await` بلا مهلة: تعثُّر السجلّ يُعلّق الدخول** |
| `6fe94ee` | شاشة الترحيب | ⚠️ **Needs review** | الانتقال صار **بيد المستخدمة** — وهي خلف `await` السابق، فإن تعثّر لم تظهر الشاشة أصلًا |
| `76c5e0c` | استثناء الأدمِن | ⚠️ **Needs review** | صحيحٌ في ذاته، **لكنه واحدٌ من ثلاثة** — والموضعان الآخران باقيان |

### ⚠️ خطرٌ لم أذكره في تقريرٍ سابق

`2daca4f` جعل الدخول **ينتظر** كتابة السجلّ:

```ts
await Promise.all([ insert(login_logs), rpc(touch_last_active) ]);
```

**ولا مهلة ولا إلغاء.** فإن بطؤت القاعدة أو تعثّرت الشبكة، **يقف
الدخول عند «جارٍ الدخول…»** — وقد كان قبله لا يُعطّل أحدًا.

⚠️ **فأنا حوّلت سجلًّا اختياريًّا إلى شرطٍ في مسار الدخول** — وهو عين
ما تعهّدتُ في التقرير ألّا يقع: «السجلّ خبرٌ عن الدخول لا شرطٌ له».
**كتبتُ العهد في التعليق ونقضتُه في السطر التالي.**
