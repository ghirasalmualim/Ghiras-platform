# تبعيات
/node_modules

# مخرجات البناء
/.next/
/out/
/build

# بيئة محلية (لا تُرفع أبدًا)
.env
.env.local
.env*.local

# أدوات محلية
/.claude

# نظام التشغيل والمحررات
.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# مجلد مؤقت لاختبارات قسم القرآن
.quran-test
.acc-test

# مخرجات Playwright المؤقتة (Stage 11 قبول الجوال)
/test-results
/playwright-report
